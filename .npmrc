# NPM Configuration for EAS Builds
# This ensures compatibility with Daily.co packages

# Use legacy peer deps to resolve conflicts
legacy-peer-deps=true

# Ensure consistent package resolution
package-lock=true

# Use npm registry
registry=https://registry.npmjs.org/
